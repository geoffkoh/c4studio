# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Layout sidecars are per-user UI state, not source
*.layout.json

# npm workspace installs. The *built* SPA under
# src/c4studio/webapp/static/ is committed deliberately (the wheel
# ships a working UI); packages/diagram-core/dist is a build artefact and
# is covered by the dist/ rule above.
node_modules/

# TypeScript incremental build state
*.tsbuildinfo
